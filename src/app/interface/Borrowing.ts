import { Reader } from './User';
import { Copy } from './Copy';

export class Borrowing {

    borrowingId: number;
    startDate: Date;
    readerId: number;
    copyId: number;
    title: string;
    
}